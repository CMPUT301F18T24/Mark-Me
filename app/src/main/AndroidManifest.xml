<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cybersix.markme">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SignupActivity"
            android:parentActivityName=".LoginActivity" />
        <activity android:name=".AccountSettingsFragment" />
        <activity android:name=".SettingsFragment" />
        <activity android:name=".PatientAssignmentActivity" />
        <activity android:name=".RecordInfoActivity"></activity>
        <activity android:name=".LiveCameraActivity" android:exported="true"></activity>
        <activity android:name=".ProblemActivityCreation" android:theme="@style/AppTheme.PopUp"></activity>
        <activity android:name=".Reminder" />
        <activity android:name=".GalleryFragment"></activity>
    </application>

</manifest>